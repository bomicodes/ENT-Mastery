ENT Mastery v10.0.3 — Runtime + Interpretation + Anatomy Visual Hotfix

Fixes:
- Restores the missing _teaching_layers.html partial that caused Interpretation Atlas lab routes to 500.
- Keeps the v10.0.1 difflib/runtime and Daily Path progression fixes.
- Adds stable /concept/id/<concept_id> routing for Daily Path concept links.
- Makes Concept Hub relationship building fail-safe instead of taking down the whole page.
- Adds 50 original orientation schematic SVGs — one for every Anatomy Atlas card.
- Keeps the v10.0.2 Orient → Protect → Apply Anatomy UX.
- Forces 0% mastery ring to render as fully empty.

Audit:
- 50 anatomy cards / 50 visuals
- Template parse errors: 0
- Missing template includes: 0

The anatomy schematics are intentionally simplified, original orientation diagrams and are labeled
'not to scale'. They are meant to support surgical orientation, not replace a formal atlas.
