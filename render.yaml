services:
  - type: web
    name: ent-mastery
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn runtime_entry:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
